# 研究方向总结（v3 修正版）：无对局数据条件下组合对抗体系的胜率评估方法

> 本版替代《方向.md》与《方向·第二部分》。相对前两版的九项修正见第〇节；其余内容继承前两版结论，仅做表述统一。

---

## 〇、本版修正清单（相对 v2 的九处变更）

| # | 修正项 | 原因 |
| --- | --- | --- |
| 1 | 概率层由"严格 Bradley-Terry 标量差式"改为**成对 logit 形式** | 克制是成对关系属性（依赖双方配置），无法分解为两个标量强度之差；且标量模型蕴含传递性，与风格克制的循环结构冲突 |
| 2 | 新增**不可传递性诊断实验** | BT 类模型的传递性假设在多策略对抗中常不成立，需实证检验而非默认 |
| 3 | 校准叙事重写：撤下 Kennedy–O'Hagan "同构"表述，改锚 **surrogate/metamodel 谱系 + VV&A** | KOH 校准以真实观测数据为定义性前提，本场景无观测数据，术语不成立 |
| 4 | "客观参照物"改为"**冻结、可审计、可复现的参照物**"；明确校准语义 = 两种知识表示的一致性对齐 | 模拟器含大量设计选择，"客观""不含主观权重"的资格声明站不住 |
| 5 | GP 叙事修正：覆盖大组合空间的根据是**机理先验压缩有效维度 + 残差光滑性兜底**，并新增残差光滑性前置实验 | GP 只在低维光滑问题上具备小样本覆盖能力，功劳不应记在 GP 本身 |
| 6 | 专家盲测（看回放 vs 真实录像）改为**统计指纹比对 + 专家行为模式审查** | 抽象回合制模拟器无画面输出，图灵测试式设计不成立 |
| 7 | 明确产出形态：任意对阵的胜率区间查询 + **给定候选池**的排序；排序停止准则仅适用于候选池 | 全空间排序既不可能也无意义，原文表述含混 |
| 8 | 相关工作补齐三条邻近谱系并逐一划界（数据驱动阵容预测 / 仿真代理模型与实验设计 / 游戏评估函数自校准与协变量 Elo） | novelty 防线原只处理了第一条谱系 |
| 9 | 表述降调：VV&A 改为"被承认的替代验证手段组合"；旧范式改为"主观性强、验证基础薄弱"；加权最小二乘不再称"最大似然"；删去"首次"式绝对声明 | 原文绝对化表述无直接文献支撑，易被审稿人击穿 |

另修正一处勘误自身的错误：组合空间"计入分路约 4×10¹⁹"对应的乘数是双方各排分路 (5!)²=14400，不是 ×5!。

---

## 一、研究问题定义

**给定一个尚未交战的配置（双方 5v5 阵容 / 无人集群编成），在无真实对局数据条件下，输出：**
1. 任意对阵的胜率估计及其置信区间；
2. 优势来源的可解释归因；
3. 给定候选配置池内的排序与 What-if / 优化建议。

形式化：求评估函数 $\pi(\text{配置}_A,\ \text{配置}_B,\ \text{环境}) \in (0,1)$。

**硬约束**：① 不用实际交战数据训练（模拟数据与"真实数据只验不拟合"的合法性需导师确认，见 §十二）；② 算力限于 CPU 级 + 消费级 GPU；③ 单人研究；④ 技能树须民用可迁移。

| 核心难点 | 对策 |
| --- | --- |
| 无真实数据 → 无验证闭环 | 冻结模拟器作参照物 + 两层验证体系 |
| 组合空间 ~10¹⁵–10¹⁹ | 机理先验压缩有效维度 + 残差 GP 代理 + 主动模拟预算分配 |
| 输出无语义 | 成对 logit 概率锚（单调/有界/对称/可校准） |
| 机理参数拍脑袋 | 模拟一致性校准 + 敏感性分析 |

---

## 二、总体架构：三层对象、两条更新线、两层验证

| 层 | 对象 | 本质 | 角色 |
| --- | --- | --- | --- |
| 公式层 | 结构化评估函数 $f$ | 静态、可解释的打分函数 | 被校准对象，输出最终答案 |
| 推演层 | 裁决模拟器 | 动态行为规则（状态机 + 结算公式） | **冻结、可审计、可复现的参照物**（非"客观标准答案"，见 §六） |
| 代理层 | 残差 GP 代理 | 模拟器的统计近似 | 不确定度量化与主动选点 |

**数据流**：模拟器产出对阵胜率 → A 线校准 $f$ 的参数 $\theta$、B 线更新 GP → GP 按信息价值选出下一个模拟点 → 循环。

**两层验证（顺序不可颠倒）**：第一层验证模拟器保真度；第二层验证校准有效性。第一层不过关时，第二层是在精确对齐一个错误的参照物。

---

## 三、结构化评估函数 $f$

### 3.1 成对强度差函数

$$\Delta_\theta(A,B) = \underbrace{\sum_{i} q(a_i) - \sum_{j} q(b_j)}_{\text{个体能力差}} + \underbrace{\sum_k c_k\,\varphi_k(A,B)}_{\text{机制类克制交互}} + \underbrace{\mathrm{syn}(A) - \mathrm{syn}(B)}_{\text{协同差}} - \underbrace{\big[\mathrm{conf}(A) - \mathrm{conf}(B)\big]}_{\text{资源冲突差}} - \underbrace{\big[\mathrm{gap}(A) - \mathrm{gap}(B)\big]}_{\text{结构缺口差}}$$

其中 $\varphi_k(A,B)$ 为成对机制特征（如"A 方突进密度 × B 方无位移占比"），满足 $\varphi_k(A,B) = -\varphi_k(B,A)$，从而整体满足**反对称性** $\Delta_\theta(A,B) = -\Delta_\theta(B,A)$。

### 3.2 各聚合项（同 v2，全部从属性表自动计算）

| 项 | 公式 | 说明 |
| --- | --- | --- |
| 混伤互补 | $\mathrm{syn}_{\text{dmg}} = w_1[1-(2p_{\text{phys}}-1)^2]$ | 伤害配比均衡度 |
| 控制链 | $\mathrm{syn}_{\text{cc}} = w_2 \cdot \max_{\text{chain}} \sum t_{\text{ctrl}}$ | 成对条件关系建图求最长路 |
| 经济冲突 | $\mathrm{conf} = w_3[\sum_i e(a_i) - E_{\text{budget}}]_+$ | 铰链函数，超预算部分才罚 |
| 结构缺口 | $\mathrm{gap} = \sum_d w_d \max(0, \tau_d - c_d(A))$ | 功能维度木桶项 |

**设计铁律（不变）**：一切成组效应经"机制类/功能维度"自动展开，绝不逐对手填；手填的只有属性表、机制类定义与维度阈值。

---

## 四、概率层：成对 logit 形式（本版重大修正）

$$P(A\text{ 胜 }B) = \sigma\big(\beta \cdot \Delta_\theta(A,B)\big) = \frac{1}{1+e^{-\beta\Delta_\theta(A,B)}}$$

### 4.1 为什么放弃严格 Bradley-Terry 差式

v2 的形式 $P = \sigma(\beta(s_A - s_B))$ 要求胜率由两个标量强度决定，存在两个问题：

1. **结构上装不下克制**：克制修正项（$c_k \cdot \varphi_k(A,B)$）本质上同时依赖双方配置，无法分解为 $g(A) - g(B)$；
2. **传递性假设与风格克制冲突**：标量强度模型蕴含传递性（A>B、B>C ⇒ A>C），而成对比较文献已指出该假设在多技能/多策略对抗中常不成立；PvP 游戏阵容克制研究明确指出 BT 类模型无法捕捉"石头剪刀布"式循环克制。

### 4.2 修正后保留的性质

成对 logit 保留了 v2 要求的全部四个性质：**单调**（强度差大→胜率高）、**有界**（恒在 (0,1)）、**对称**（反对称性保证 $\Delta=0$ 时恰为 50%）、**可校准**（预测概率可与模拟频率对账）。代价是放弃"严格 Elo 同构"的修辞。

**与相关文献的划界**：带协变量的 Elo/BT 扩展已存在（用属性参数化能力），但其能力参数仍由历史胜负标签估计；本课题无任何胜负标签，先验只能来自机理与模拟——新颖性锚点在此，不在"从特征算强度"。

### 4.3 不可传递性诊断（新增实验）

- 用模拟器在采样子集上产出完整胜率矩阵，统计循环三角（A>B、B>C、C>A）的频率与强度；
- 若循环成分显著：克制交互项 $\varphi_k$ 已提供表达循环的通道（成对特征天然可表达 A 克 B、B 克 C、C 克 A），必要时引入低秩对抗矩阵扩展；
- 若循环成分轻微：成对 logit 是对真实结构的良好近似，如实报告诊断结果。

该诊断同时是论文的一个实证贡献点：**在受控模拟环境中测量阵容对抗的不可传递性程度**。

---

## 五、裁决模拟器（蒙特卡洛推演）

定义与三层随机化原则继承 v2：

| 层 | 内容 | 处理方式 |
| --- | --- | --- |
| 机制层 | CD、伤害公式、经济规则 | 确定性硬编码，不许随机 |
| 决策/状态层 | 站位、开团时机、目标选择 | 状态的函数 + 小噪声 |
| 执行层 | 命中偏差、走位抖动、时机误差 | 随机变量，分布参数对应能力 $\lambda$ |

- 微策略 = 4–5 条维度完备的最小原则集，打法作为涌现物；补漏靠分类学对照、极端阵容自测、行为统计监控；
- 能力参数 $\lambda$：初始双方相等以量出阵容固有强弱，之后作为显式维度产出"胜率-能力曲线"，暴露**阵容×能力交互效应**（本课题独有的分析维度，数据驱动路线反而难以获得）；
- 蒙特卡洛口径：$N \approx 2000$、$p\approx0.5$ 时 95% 置信半宽约 ±2.2%。

**资格声明（措辞修正）**：模拟器的参照物资格来自"冻结、可审计、可复现"——固定版本下规则不再变动、逐回合可回放审计、固定种子可复现；而不是"客观、不含主观权重"。模拟器不含经数据拟合得到的权重，但包含大量设计选择（微策略原则、噪声分布与幅度），这些设计选择编码的正是领域知识。**模拟器经涌现验证迭代升级后，下游校准必须重做。**

---

## 六、校准回路（语义重写）

### 6.1 校准的真实语义

校准回路消除的是**同一套机理知识的两种表示之间的不一致**：公式层（静态函数）与推演层（动态规则）都是对领域知识的编码，二者输出对不上时，至少有一种表示错了。校准使两种表示对齐；**对现实世界的锚定不由校准承担，而由第一层验证（尤其公开数据统计锚定）单独承担**。

### 6.2 估计方法（表述修正）

预测链：$\hat{p}_i = \sigma(\beta \cdot \Delta_\theta(x_i))$；$\theta$ 为几十个机理参数。

估计采用**分组二项 logistic 似然 + L2 正则**（即后验众数/MAP 估计）：

$$L(\theta) = -\sum_i N_i\left[\hat{y}_i \log \hat{p}_i(\theta) + (1-\hat{y}_i)\log(1-\hat{p}_i(\theta))\right] + \lambda\|\theta\|^2$$

$N_i$ 为对阵 $i$ 的模拟局数（自动实现异方差加权：局数多的对阵权重更大）。L-BFGS 求解。不再使用"加权最小二乘=最大似然"的表述（高斯近似成立但不够严格，且加正则后严格说是 MAP 而非 MLE）。

### 6.3 诊断纪律（不变）

参数纠缠 → 正则化 + 参数数 ≪ 对局数 + 敏感性分析；残差分组诊断 → 某机制组系统性偏差 = 机制写错，回炉修结构，**不许让参数硬吸**。

### 6.4 文献锚点（修正）

- 方法论谱系：**仿真代理模型 / 元模型（surrogate / metamodel）+ VV&A 验证范式**；
- Kennedy–O'Hagan (2001) 保留于相关工作，表述为"若未来获得真实观测数据，本框架可自然升级为标准的贝叶斯校准（含模型失配项建模）"——作为扩展路径而非当前归属。

---

## 七、GP 代理与主动模拟（叙事修正）

### 7.1 覆盖大组合空间的真正根据

不是 GP 本身，而是**两级压缩**：

1. 机理骨架 $f$ 把阵容对映射到十几个结构化特征（强度差、混伤均衡度、控制链长、克制特征强度……），将有效维度从天文数字压缩到低维空间；
2. 残差 GP 学习"模拟胜率 − $f$ 预测"的偏差——机理已解释的部分不浪费数据，GP 只负责光滑残差的插值与不确定度量化。

**前提**：残差足够小且光滑。因此新增**前置实验**：用初始 30–50 个模拟点拟合残差 GP，报告残差方差占比与长度尺度，作为"两级压缩成立"的实证。该实验本身可成为论文的一张证据图。

**失效备案**：若残差不光滑，退化为"机理骨架 + 局部模拟"，产出从"全空间查询"收缩为"候选池评估"，课题故事依然成立。

### 7.2 GP 性质的表述降调

GP 的不确定性估计在模型设定正确（核函数与光滑性假设成立）时是校准的；模型误设时同样会过度自信。选择 GP 的理由是相对神经网络在小样本上更可控、后验解析、超参可由边缘似然估计。

### 7.3 主动选点与停止条件（澄清）

- 选点：优先"预测胜率接近但置信区间都宽"的对局（对排序的信息价值最大）；
- 停止条件：① 查询集方差低于阈值；② **给定候选池内**相邻排名的置信区间不再重叠（本判据仅适用于候选池，不承诺全空间排序）；③ 预算耗尽。

---

## 八、产出形态（澄清）

最终交付三件套：

1. **任意对阵的胜率区间查询**（经 GP 代理，外推区域只报大方差不报点值）；
2. **给定候选池的排序**（候选池来自贝叶斯优化生成的 top-k 方案或人工备选）；
3. **可解释归因与改进建议**（结构化模型每项对应可查机制）。

不承诺对全组合空间的完整排序。

---

## 九、两层验证体系（修正）

### 9.1 第一层：模拟器保真度验证（五道关，第三关修改）

| 关 | 做什么 |
| --- | --- |
| 结构有效性 | 参数响应方向检验（加强英雄→胜率升；克制上场→被克方降） |
| 极端案例 | 全坦/全辅助/无控制阵容行为是否荒谬 |
| ~~专家盲测回放~~ → **统计指纹比对** | 比对模拟对局与真实对局的分布级统计量：对局时长分布、经济曲线形态、击杀参与率、**阵容胜率排序的 Kendall's τ** 等；专家审查对象改为**决策序列与行为模式的合理性**（不看画面） |
| 交叉验证 | 独立实现的另一套裁决规则重跑，检验排序稳健性（注明：同一作者实现，证据等级有限） |
| 公开数据锚定 | 公开赛事 BP+胜率**只验不拟合**（须导师确认，见 §十二） |

**继承 VV&A 的表述（降调）**：VV&A 为无真值场景提供了一套被承认的替代验证手段（表面效度、概念模型验证、敏感性分析等），本课题组合使用并显式报告其证据等级低于实证验证的局限。

### 9.2 第二层：校准有效性验证

留出未参与校准的阵容对做校准曲线检验（预测 69% 的组，实际频率是否落在附近）。

### 9.3 保真度阶梯与收敛判定（不变）

L0→L4 逐层加细节，以排序 Kendall's τ 是否显著变化为唯一收敛判据，停在收敛点；细化只沿"影响阵容相对强弱"的维度走。

### 9.4 认识论定位（不变，全文核心姿态）

模拟器是"机理假设的显式化推演机"，不是"现实的镜子"。输出读作"给定机理假设下的逻辑推论 + 对假设扰动的稳健性报告"，不声称绝对现实胜率。

---

## 十、与三条邻近谱系的划界（新增章节）

| 谱系 | 代表 | 本课题的划界 |
| --- | --- | --- |
| 数据驱动阵容预测 | DraftRec、The Art of Drafting、JueWuDraft | 均依赖海量历史对局标签；本课题无此前提，且将其保留为实验对照组 |
| 仿真代理模型 + 实验设计 | 防空导弹仿真代理（Wade 2019）、兵棋推演 DOE、贝叶斯优化自适应仿真 | 工具层成熟可继承，但其对象是连续参数空间，不覆盖"组合对抗配置评估 + 概率语义输出 + 可解释归因" |
| 游戏评估函数自校准 | Samuel 跳棋（1959）→ TD-Gammon → Bootstrapping from Game Tree Search | 目的不同：该谱系造评估函数是为了"下得赢"（评估服务搜索与决策，从不单独接受检验）；本课题造评估函数是为了"测得准"（评估本身是产品，须带误差、可归因、可被外部检验） |
| 协变量 Elo/BT | 足球能力协变量建模等 | 能力参数仍由历史胜负标签估计；本课题无标签，先验只能来自机理与模拟 |

---

## 十一、被否定的路径（存档，新增 E18–E20）

E1–E17 继承前两版（禁一切数据驱动 / 兰彻斯特聚合 / 错误发表规划 / 无锚评分 / 决策层纯随机 / 打法清单 / 引擎级模拟 / CVPR 主战场 / 纯大模型应用层 / 监督学习拟合历史对局[保留为对照组] / 多领域全做 / novelty 锚在工具上 / 工程报告式论文 / 残差异常调参硬吸 / 预期不符即判模拟器错 / 通用平台当目标 / 换领域从零再造）。

| # | 被否定做法 | 错误理由 |
| --- | --- | --- |
| E18 | 严格 BT 标量差式概率层（$\sigma(s_A-s_B)$） | 克制是成对关系属性，装不进标量差；传递性假设与风格克制冲突 |
| E19 | "客观参照物"措辞 + KOH "同构/校准"术语 | 模拟器含设计选择，资格只能来自冻结/可审计/可复现；KOH 校准以观测数据为前提，无真值场景应锚定 surrogate/metamodel 谱系 |
| E20 | 专家看模拟回放 vs 真实录像的盲测设计 | 抽象回合制模拟器无画面输出，区分度恒为 100%，测试不携带信息；改为统计指纹比对 + 行为模式审查 |

---

## 十二、风险与待确认清单（按优先级）

1. **【最高优先级，先于一切代码】公开赛事数据"只验不拟合"的边界须与导师当面确认**——它决定第一层验证有无实证锚，进而决定论文主张的层级；
2. 模拟器涌现质量（Phase 1 为弹性区间，可能超预算 2–3 倍；对策：早 sanity check、极端阵容自测、小步迭代）；
3. 不可传递性占比未知（诊断实验见 §4.3）；
4. 残差光滑性未知（前置实验见 §7.1）；
5. 专家资源（改为行为模式审查后依赖度下降，仍需 3–5 名专家参与关键节点）；
6. 沿用前版：最小原则集内容、机制类粒度、保真度阶梯收敛点、模拟器版本升级的校准失效半径、β 校准精度上限。

---

## 十三、执行路线图（Phase 0 前置一步，其余不变）

```
Phase -1（1 周内）  与导师确认公开数据验证边界          → 课题价值上限锁定
Phase 0（2 周）    英雄属性表 + L0 对撞模拟器           → 数据管道通
Phase 1（4–6 周，弹性） L1–L2 模拟器 + 常识排序 sanity check → 模拟器 v1
Phase 2（2–4 周）  评估函数 + 成对 logit 层 + 一致性校准 + 不可传递性诊断 → 校准曲线
Phase 3（2–4 周）  残差光滑性前置实验 + GP 代理 + 主动选点 + What-if demo
Phase 4（2–3 周）  统计指纹比对 + 敏感性 + (数据锚定)     → 投稿版本
```

---

## 十四、创新点陈述（修正版）

> **问题**：在无法获得任何真实交战数据的强对抗场景下，如何对组合级配置给出带置信区间、可归因、可检验的强弱评估？现有两条路都不通：专家打分类方法主观性强、验证基础薄弱；数据驱动方法（DraftRec 等）以海量对局标签为前提。
>
> **贡献 1（问题形式化）**：将该问题形式化为"结构化机理先验 + 成对概率语义输出 + 有限仿真预算下的不确定性推断"。据我们所知，尚无工作在无对局数据条件下完成这一组合。
>
> **贡献 2（方法）**：机理骨架与动态推演规则作为同一知识的两种表示，经一致性校准对齐；机理先验压缩有效维度，残差 GP 在小样本下提供校准的不确定度；信息价值驱动的主动模拟分配预算；成对 logit 结构显式容纳机制类克制，并以不可传递性诊断实证检验传递性近似的适用边界。
>
> **贡献 3（验证）**：两层验证体系（模拟器保真度 → 校准有效性），含分布级统计指纹比对，使"凭什么信"本身成为可检验的方法组成。
>
> **边界（主动声明）**：输出为机理假设下的相对排序与置信区间，不声称绝对现实胜率；若获得观测数据，框架可升级为标准贝叶斯校准。

**对"和 20 年前博弈方法有什么区别"的应答（修正版）**：区别不在工具（蒙特卡洛、GP、logistic 都是旧工具），而在两点——① 目的不同：经典游戏评估函数服务于"下得赢"，从不单独接受检验；本框架的评估本身是产品，必须带误差、可归因、可被外部检验；② 约束不同：协变量 Elo 等"从特征算强度"的模型仍有胜负标签可用，本框架在无标签条件下只能以机理+模拟构造先验，并配套自参照验证结构。

---

## 十五、发表与就业（简述，结论不变）

发表阶梯：IEEE CoG / ToG 练手 → AAMAS 主目标 → AAAI/IJCAI 应用 track 冲刺；统一叙事"有限仿真预算下的组合对抗体系可解释评估方法"。就业技能树：蒙特卡洛仿真 + 贝叶斯推断 + 不确定性量化 + 多智能体系统；出口为评测岗、游戏数值/AI、仿真评估、数字孪生。简历语言用方法论关键词。
